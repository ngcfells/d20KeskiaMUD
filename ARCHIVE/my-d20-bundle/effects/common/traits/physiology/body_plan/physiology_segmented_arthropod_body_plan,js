'use strict';

module.exports = {
  config: {
    name: "Physiology: Segmented Arthropod Body Plan",
    description: "A multi-segmented body with distributed load-bearing structure.",
    type: "trait",
    family: "physiology_segmented_arthropod_body_plan",
    unique: true,
    persists: true
  },

  state: {
    reachBonus: 0,
    carryBonus: +4,
    stabilityBonus: +2
  },

  modifiers: {
    attributes(current, state) {
      const r = {};
      if (typeof current.reach === "number") r.reach = current.reach + state.reachBonus;
      if (typeof current.carry === "number") r.carry = current.carry + state.carryBonus;
      if (typeof current.stability === "number") r.stability = current.stability + state.stabilityBonus;
      return r;
    }
  }
};
